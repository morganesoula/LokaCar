<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="ToolbarStyle" parent="AppTheme">
        <item name="android:textColorPrimary">@android:color/white</item>
        <item name="colorControlNormal">@android:color/white</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <!-- Style to all edit text -->
    <style name="EditTextTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorControlNormal">@color/line_dark</item>
        <item name="colorControlActivated">@color/colorPrimaryDark</item>
        <item name="android:maxLines">1</item>
        <item name="android:singleLine">true</item>
    </style>

    <!-- Style to all input text -->
    <style name="TextInputLayoutTheme">
        <item name="android:textColorHint">@color/colorPrimaryDark</item>
    </style>


    <!-- Style to the login button -->
    <style name="ButtonLoginTheme">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:background">@drawable/button_background</item>
        <item name="android:textColor">@color/mdtp_white</item>
    </style>

    <!-- Style to date picker button -->
    <style name="ButtonDatePickerTheme">
        <item name="android:layout_marginTop">16dp</item>
        <item name="android:textStyle">bold</item>
        <item name="android:textColor">@color/mdtp_white</item>
        <item name="android:background">@color/colorPrimaryDark</item>
    </style>


    <!-- Style to empty list message -->
    <style name="EmptyListMessageTheme">
        <item name="android:textStyle">bold</item>
        <item name="android:paddingTop">200dp</item>
        <item name="android:paddingLeft">110dp</item>
        <item name="android:visibility">gone</item>
    </style>

    <!-- Style to empty list location message -->
    <style name="EmptyListLocationMessageTheme">
        <item name="android:textStyle">bold</item>
        <item name="android:paddingTop">200dp</item>
        <item name="android:paddingLeft">43dp</item>
        <item name="android:visibility">gone</item>
    </style>

</resources>
